unit Aux.Types;

interface

  Type
    TTipoGasolina = (tgComum, tgAditivada, tgPremium);
    TTipoDiesel = (tdS10, tdS50, tdS500, tdS1800);
    TTipoImobilizado = (tiMaquinario, tiEstrutural);
    TStatus = (tsInativo, tsAtivo);

implementation

end.
